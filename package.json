{
  "name": "prospect-reach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local sales outreach automation tool. Excel library: exceljs, chosen over xlsx/SheetJS because it supports write-side cell styling (fill color, italics) and header row protection, which the blank template requires.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "generate:template": "node scripts/generate-template.js",
    "generate:brand": "node scripts/generate-brand-assets.js",
    "build:extension": "node scripts/build-extension.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
